var config = {};
config.servicenow = {};

config.servicenow.username = 'admin';
config.servicenow.password = 'your-servicenow-password';
config.servicenow.hostname = 'your-servicenow-host';
config.servicenow.port = 443;

module.exports = config;
